package com.poli;

/**
 * Clase GenerateInfoFiles
 * Entrega 1 - Principios de Programacion
 *
 * Objetivo:
 *  - Disenar la estructura de la clase que generara los archivos de prueba
 *    (vendedores, productos y ventas).
 *
 * Metodos planeados:
 *  1. createProductsFile(int productsCount)
 *     -> Generara el archivo products.txt
 *     -> Formato: IDProducto;NombreProducto;PrecioPorUnidad
 *
 *  2. createSalesManInfoFile(int salesmanCount)
 *     -> Generara el archivo salesmen_info.txt
 *     -> Formato: TipoDocumento;NumeroDocumento;Nombres;Apellidos
 *
 *  3. createSalesMenFile(int randomSalesCount, String name, long id)
 *     -> Generara un archivo de ventas por vendedor
 *     -> Cabecera: TipoDocumentoVendedor;NumeroDocumentoVendedor
 *     -> Cuerpo: IDProducto;Cantidad
 */
public class GenerateInfoFiles {
    // Clase vacia para la entrega teorica (sin implementacion por ahora).
}
